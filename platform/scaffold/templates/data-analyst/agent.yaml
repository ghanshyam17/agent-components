apiVersion: agentcomponents/v1
kind: Agent
metadata:
  name: data-analyst
  project: {{ project_name }}
spec:
  pattern: react
  model:
    provider: openai
    deployment_name: gpt-4
  tools:
    code_interpreter:
      enabled: true
      sandbox_type: local-docker
      packages:
        - pandas
        - matplotlib
